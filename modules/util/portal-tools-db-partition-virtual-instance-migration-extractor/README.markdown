# Database Partition Virtual Instance Migration Tool
This tool allows the extraction and validation of the database information needed to validate that all the conditions for a successful virtual instance migration are met.

## Requirements
- MySQL
- Database user with DDL privileges

## Usage
```
./db_partition_virtual_instance_migration.sh -e [OPERATION_MODE] [OPERATION_PARAMETERS]
```

Main operation mode:

- `-e`, `--extract` Execute data extraction.
- `-h`, `--help` Display options.
- `-v`, `--validate` Execute data validation.

Data Extraction parameters:

- `-d`, `--output-dir <arg>` Output directory. Folder ./extractions otherwise.
- `-j`, `--jdbc-url <arg>` JDBC URL.
- `-p`, `--password <arg>` Database user password.
- `-s`, `--schema-name <arg>` Database schema name to be extracted. Default schema name otherwise.
- `-u`, `--user <arg>` Database user name.

Data Validation parameters:

- `-s`, `--source-file <arg>` Source file generated by the Database Partition Virtual Instance Migration Extractor Tool.
- `-t` , `--target-file <arg>` Target file generated by the Database Partition Virtual Instance Migration Extractor Tool.

### Execution examples
```
./db_partition_virtual_instance_migration.sh -e -j "jdbc:mysql://localhost:3306/lportal?useUnicode=true&characterEncoding=UTF-8&useFastDateParsing=false&useTimezone=true&serverTimezone=GMT" -u user -p password -s lpartition_1234 -d "/migrations/extractions"
./db_partition_virtual_instance_migration.sh -v -s "/bundle/tools/portal-tools-db-partition-virtual-instance-migration-extractor/source.json" -t "/bundle/tools/portal-tools-db-partition-virtual-instance-migration-extractor/target.json"
```